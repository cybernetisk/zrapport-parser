# A sample Guardfile
# More info at https://github.com/guard/guard#readme

require 'guard/plugin'

guard :shell do
  watch(%r{~/Dropbox/UiO/Cyb/Z-rapport/\d{4}/.*\.DTT"}) do |a,b,c|
    puts a
    puts b
    puts c
  end
end
